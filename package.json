{
  "name": "hotel-ping-app",
  "version": "1.0.0",
  "private": true,
  "description": "Hotel Ping — department messaging app for hotel staff",
  "main": "server/index.js",
  "scripts": {
    "start": "node --no-warnings server/index.js",
    "test": "node --no-warnings tests/hotel-isolation-and-delivery.test.js"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
